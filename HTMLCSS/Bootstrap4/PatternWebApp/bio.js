// These functions will rotate the images from my Bio page,
// 90 degrees!

function rotateImageOne() {
    var img = document.getElementById('imgOne');
    img.style.transform = 'rotate(180deg)';
}

function rotateImageTwo() {
    var img = document.getElementById('imgTwo');
    img.style.transform = 'rotate(180deg)';
}

function rotateImageThree() {
    var img = document.getElementById('imgThree');
    img.style.transform = 'rotate(180deg)';
}

